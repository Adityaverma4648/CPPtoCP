#include <bits/stdc++.h>
using namespace std;
int main()
{
    string x, rx;
    cin >> x;
    int size = x.length();
    for (int i = 0; i < x.length(); i++)

    {
        rx[i] = x[x.length() - i];
    }

    cout << rx << endl;
    return 0;
}